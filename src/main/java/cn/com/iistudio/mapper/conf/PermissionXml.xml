<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.com.iistudio.mapper.PermissionsMapper">
<update id="addPermission" parameterType="String">
   update User set Privilege+=1 where username=#{username}
</update>

<update id="increasePermission" parameterType="String">
   update User set Privilege-=1 where username=#{username}
</update>

<update id=" deleteone" parameterType="String">
   delete from User,MembersUser where username=#{username};
</update>

</mapper>