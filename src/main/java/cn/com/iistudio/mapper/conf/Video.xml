<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.com.iistudio.mapper.VideoMapper">
<select id="getAll" resultType="cn.com.iistudio.entity.VideoResources">
   select * from VideoResources order by Pubdata desc
</select>

<select id="getUserVideo" resultType="cn.com.iistudio.entity.VideoResources" parameterType="String">
   select *from VideoResources where UserName=#{UserName} order by Pubdata desc
</select>

<select id="getId" resultType="cn.com.iistudio.entity.VideoResources" parameterType="int">
   select *from VideoResources where Id=#{Id}
</select>

<select id="getnumber" resultType="cn.com.iistudio.entity.ArticleResources" parameterType="int">
   SELECT * FROM (SELECT * FROM VideoResources ORDER BY VideoResources.Pubdata) as t LIMIT 0,#{number} 
</select>
</mapper>